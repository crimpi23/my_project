<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:g="http://base.google.com/ns/1.0" version="2.0">
  {% for lang, products in products_by_lang.items() %}
  <channel>
    <title>AutogroupEU Products - {{ lang|upper }}</title>
    <link>{{ domain }}/{{ lang }}</link>
    <description>Auto parts catalog - {{ lang|upper }}</description>

    {% for product in products %}
    <item>
      <g:id>{{ product.article }}_{{ lang }}</g:id>
      <g:title>{{ product.name }}</g:title>
      <g:description>{{ product.description }}</g:description>
      <g:link>{{ domain }}/{{ lang }}/product/{{ product.article }}</g:link>
      <g:image_link>{{ product.image_url }}</g:image_link>
      <g:price>{{ "%.2f"|format(product.price|float) }} EUR</g:price>
      <g:availability>in stock</g:availability>
      <g:condition>new</g:condition>
      <g:brand>{{ product.brand_name if product.brand_name else 'AutogroupEU' }}</g:brand>
      <g:google_product_category>Vehicle Parts &amp; Accessories</g:google_product_category>
      <g:language>{{ lang }}</g:language>
    </item>
    {% endfor %}
  </channel>
  {% endfor %}
</rss>